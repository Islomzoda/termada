{
  "name": "termada",
  "description": "Reliable, transparent terminal runtime for AI agents — persistent local/SSH sessions, async jobs, interactive prompts, structured output, and a human dashboard with policy/approval/audit. Bundles the termada MCP server config and the usage skill. Requires the `termada` binary on PATH (brew install Islomzoda/tap/termada, or a GitHub release).",
  "version": "0.8.1",
  "author": {
    "name": "Islomzoda",
    "url": "https://github.com/Islomzoda"
  },
  "homepage": "https://github.com/Islomzoda/termada",
  "repository": "https://github.com/Islomzoda/termada",
  "license": "Apache-2.0",
  "keywords": ["mcp", "terminal", "shell", "ssh", "agent", "pty"],
  "mcpServers": {
    "termada": {
      "command": "termada",
      "args": ["serve", "--stdio"]
    }
  }
}
